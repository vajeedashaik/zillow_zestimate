I have a Zillow Zestimate residual error prediction ML project. Here is full context:

PROJECT SUMMARY:
- Goal: Predict logerror = log(Zestimate) - log(SalePrice)
- Best model: Stacked ensemble; XGBoost used for SHAP (TreeExplainer already implemented in Model.py)
- Key features with SHAP values: tax_ratio, geo_cluster_te, living_area_ratio, property_age, county_te, structure_land_ratio, geo_cluster_density, month_sin, age_cluster_interaction
- Existing site: Next.js 16 + Tailwind CSS + Recharts, dark theme

TASK:
Build a self-contained SHAP explainability page for the showcase website where a user can input property details and see a visual breakdown of what drove the model's logerror prediction.

REQUIREMENTS:
1. Input form with fields: Year Built, Finished Sq Ft, Lot Size Sq Ft, Tax Amount, Tax Value ($), Transaction Month, County (dropdown: LA / Orange / Ventura)
2. On submit: compute the engineered features client-side (property_age = 2016 - yearbuilt, tax_ratio, living_area_ratio, etc.) and call a mock SHAP explainer that returns realistic SHAP values
3. Visualisations to show:
   a. Waterfall chart — each feature's contribution pushing prediction up (red) or down (blue) from base value
   b. Horizontal bar chart — absolute SHAP values ranked by importance
   c. Prediction summary card: final logerror value, interpretation text ("Zestimate likely overestimated by ~X%"), confidence band
4. Interpretation text should be human-readable: e.g. "High tax ratio increased error by +0.023 — properties with disproportionate tax burdens tend to be overestimated"
5. Use Recharts for all charts (already installed in the project)
6. Match existing dark theme styling
7. Add a "Surprise me" button that fills the form with a random realistic property

OUTPUT:
- app/explainer/page.tsx
- components/WaterfallChart.tsx
- components/ShapBarChart.tsx
- components/PredictionCard.tsx
- lib/shapMock.ts (mock SHAP value generator based on feature inputs)
- Add the Explainer page link to the existing Navbar.tsx